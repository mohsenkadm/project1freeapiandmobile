name: dayni
description: تطبيق دَيني — إدارة ديون الزبائن للمحلات والأعمال الصغيرة في العراق.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  drift: ^2.31.0
  drift_flutter: ^0.2.8
  equatable: ^2.1.0
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_animate: ^4.5.2
  flutter_riverpod: ^2.6.1
  go_router: ^17.2.3
  google_fonts: ^8.1.0
  intl: any
  path: ^1.9.1
  path_provider: ^2.1.5
  share_plus: ^12.0.2
  sqlite3_flutter_libs: ^0.5.42
  url_launcher: ^6.3.2
  uuid: ^4.6.0
  sqlite3: ^2.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  drift_dev: ^2.31.0
  build_runner: ^2.4.13
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
  generate: true
